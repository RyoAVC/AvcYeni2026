import { sql } from "drizzle-orm";
import { infrastructureResources } from "../../../../../../db/schema";
import { logAdminAction } from "../../../../../audit-log.mjs";
import { authorizeControlDesk, controlDeskJson, hasControlDeskRole } from "../../../../../control-desk-auth.mjs";
import { ensureCommerceLicenseTables } from "../../../../../local-d1-schema.mjs";

const text=(value:unknown,max=300)=>String(value??"").trim().slice(0,max);
async function hostingerGet(base:string,token:string,path:string){const controller=new AbortController(),timeout=setTimeout(()=>controller.abort(),15000);try{const response=await fetch(`${base}${path}`,{headers:{Accept:"application/json",Authorization:`Bearer ${token}`},signal:controller.signal});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(`Hostinger ${path} HTTP ${response.status}`);return body;}finally{clearTimeout(timeout);}}
const rowsOf=(body:any)=>Array.isArray(body)?body:Array.isArray(body?.data)?body.data:[];
export async function POST(request:Request){const auth=await authorizeControlDesk(request);if(!auth.ok)return controlDeskJson({ok:false,error:auth.error},auth.status,request);if(!hasControlDeskRole(auth,["platform_owner"]))return controlDeskJson({ok:false,error:"Hostinger senkronizasyon yetkiniz yok."},403,request);const token=text(auth.env.HOSTINGER_API_TOKEN,500);if(!token)return controlDeskJson({ok:false,error:"Hostinger API tokenı merkezi sır kasasında tanımlı değil."},409,request);const body=await request.json().catch(()=>({})) as Record<string,unknown>,customerId=Number(body.customerId);if(!Number.isSafeInteger(customerId)||customerId<1)return controlDeskJson({ok:false,error:"Kaynakların bağlanacağı müşteri seçilmelidir."},400,request);const base=text(auth.env.HOSTINGER_API_BASE,300)||"https://developers.hostinger.com";if(!/^https:\/\/developers\.hostinger\.com\/?$/i.test(base))return controlDeskJson({ok:false,error:"Hostinger API adresi güvenli değil."},500,request);try{const [websitesBody,vpsBody]=await Promise.all([hostingerGet(base,token,"/api/hosting/v1/websites"),hostingerGet(base,token,"/api/vps/v1/virtual-machines")]);const websites=rowsOf(websitesBody),machines=rowsOf(vpsBody),now=new Date().toISOString();await ensureCommerceLicenseTables(auth.env);const {getDb}=await import("../../../../../../db");const db=getDb();for(const item of websites){const domain=text(item.domain,253).toLowerCase(),providerResourceId=`website:${text(item.order_id||domain,160)}`;await db.insert(infrastructureResources).values({customerId,provider:"hostinger",providerResourceId,type:"hosting",name:domain||"Hostinger web sitesi",domain,status:item.is_enabled===false?"offline":"active",planName:text(item.website_type,80),region:"",lastSyncedAt:now,createdAt:now,updatedAt:now}).onConflictDoUpdate({target:[infrastructureResources.provider,infrastructureResources.providerResourceId,infrastructureResources.customerId],set:{name:domain||"Hostinger web sitesi",domain,status:item.is_enabled===false?"offline":"active",planName:text(item.website_type,80),lastSyncedAt:now,updatedAt:now}});}for(const item of machines){const id=text(item.id,100),providerResourceId=`vps:${id}`;await db.insert(infrastructureResources).values({customerId,provider:"hostinger",providerResourceId,type:"vps",name:text(item.hostname||item.name,160)||`VPS ${id}`,status:text(item.state||item.status,40)||"unknown",planName:text(item.plan||item.plan_name,100),region:text(item.datacenter?.name||item.location,100),ipAddress:text(item.ipv4?.[0]||item.ip_address,80),lastSyncedAt:now,createdAt:now,updatedAt:now}).onConflictDoUpdate({target:[infrastructureResources.provider,infrastructureResources.providerResourceId,infrastructureResources.customerId],set:{name:text(item.hostname||item.name,160)||`VPS ${id}`,status:text(item.state||item.status,40)||"unknown",planName:text(item.plan||item.plan_name,100),region:text(item.datacenter?.name||item.location,100),ipAddress:text(item.ipv4?.[0]||item.ip_address,80),lastSyncedAt:now,updatedAt:now}});}await logAdminAction(db,{userEmail:auth.email,action:"hostinger_inventory_synced",entity:"customer",entityId:String(customerId),details:JSON.stringify({websites:websites.length,vps:machines.length})});return controlDeskJson({ok:true,format:"avci-control-desk.hostinger-sync.v1",customerId,counts:{websites:websites.length,vps:machines.length},syncedAt:now},200,request);}catch(cause){console.error("Hostinger inventory sync failed",cause instanceof Error?cause.message:"unknown");return controlDeskJson({ok:false,error:"Hostinger envanteri alınamadı. Token yetkisini ve API erişimini kontrol edin."},502,request);}}
